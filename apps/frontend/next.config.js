/** @type {import('next').NextConfig} */
const nextConfig = {
  // Sentry configuration will be added by @sentry/nextjs plugin
  // Run: npx @sentry/wizard@latest -i nextjs
}

module.exports = nextConfig

